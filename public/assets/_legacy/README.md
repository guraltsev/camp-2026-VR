# Legacy Public Assets

This directory contains the archived ad hoc asset bundle that the current code
still loads.

When the new model set lands, it should use a more systematic structure in
`public/assets/` instead of extending this archive.
