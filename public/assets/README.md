# Public Assets

This directory is the landing zone for the next, more systematic asset set.

The older ad hoc bundle has been archived under `public/assets/_legacy/` and
is still what the current code loads. New uploads should use a cleaner,
deliberate layout when they arrive.
